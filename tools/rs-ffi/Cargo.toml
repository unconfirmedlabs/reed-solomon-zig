[package]
name = "rs-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
reed-solomon-simd = { path = "../vendor/reed-solomon-simd" }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
